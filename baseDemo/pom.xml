<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 继承 Spring Boot 4 父 pom，统一管理 Spring Boot 依赖版本 -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.8</version>
        <relativePath/>
    </parent>

    <groupId>com.dms21</groupId>
    <artifactId>baseDemo</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>baseDemo</name>
    <description>DMS21 base demo: eureka + serviceA + serviceB (Spring Boot 4 + JDK 21)</description>

    <properties>
        <!-- JDK 21（LTS），Spring Boot 4 要求 17+ -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <!-- Spring Cloud 2025.1.x 对应 Spring Boot 4.0.x -->
        <spring-cloud.version>2025.1.3</spring-cloud.version>
    </properties>

    <!-- 统一管理 Spring Cloud 依赖版本，子模块无需写版本号 -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- 三个子模块 -->
    <modules>
        <module>eureka</module>
        <module>serviceA</module>
        <module>serviceB</module>
    </modules>
</project>
