server:
  port: 8081 # serviceA 端口

spring:
  application:
    name: service-a # Eureka 中的服务名

# 注册到 Eureka，并从 Eureka 拉取注册表（用于发现 service-b）
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
